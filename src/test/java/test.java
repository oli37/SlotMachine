import application.Slot;
import org.junit.Rule;
import org.junit.Test;
import org.junit.rules.ExpectedException;

import static junit.framework.TestCase.assertEquals;

public class test {
    /**
     * Testing a private class
     *
     *  @Test
     *     public void testRoundMinuteToQuarterHour() {
     *         Slot slot = new Slot();
     *
     *         // assert statements
     *         assertEquals(0, slot.roundMinuteToQuarterHour(14));
     *         assertEquals(15, slot.roundMinuteToQuarterHour(29));
     *         assertEquals(30, slot.roundMinuteToQuarterHour(44));
     *         assertEquals(45, slot.roundMinuteToQuarterHour(59));
     *     }
     *
     *
     *     @Test(expected = AssertionError.class)
     *     public void testRoundMinuteToQuarterHourException() {
     *         Slot slot = new Slot();
     *         slot.roundMinuteToQuarterHour(61);
     *
     *     }
     */


}
